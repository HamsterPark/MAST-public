"""mast.core — shared types, state, registry, safety primitives.

Phase 0: placeholder.
Phase 3 fills this in with ports of v1 mast/core/ (types, state, registry).
safety middleware lives in mast/agents/_shared/safety_mw.py, not here.
"""
