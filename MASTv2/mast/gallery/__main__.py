# -*- coding: utf-8 -*-
"""``python -m mast.gallery build|config|import-marks|status`` —— 见 :func:`mast.gallery.build.main`。"""
from mast.gallery.build import main

if __name__ == "__main__":
    raise SystemExit(main())
